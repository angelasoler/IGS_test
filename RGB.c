int main(void)
{
    char    r_inicial;
    char    r_final
    int     steps;

    scanf("cor inicial: %s\ncor final: %s\nsteps: %d", r_inicial, r_final, steps);
    print_steps(r_inicial, r_final, steps);
    return(0);
}